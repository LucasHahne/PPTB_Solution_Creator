# Build Plan

List the features that make up your project, high level and in rough build order.
Keep each item to one line; the details come later in `/feature`.

Shipped items are checked so `/status` and `/feature` point at what is next.
Do not renumber completed features; archived specs refer to those IDs.

## Your features

- [x] 1. **Solution setup** - create or reuse unmanaged solution and publisher
- [x] 2. **Table design** - add custom tables with ownership, notes, activities
- [x] 3. **Column builder** - spreadsheet-style fields with creatable Dataverse types
- [x] 4. **Choice columns** - local, multi-select, and global choice management
- [x] 5. **Column schema JSON** - export, template, and import/merge by schema name
- [x] 6. **1:N lookups** - relationships with cascade config and env table search
- [x] 7. **Review and deploy** - validation, ordered deploy, live log, publish
- [x] 8. **Draft autosave** - persist and restore via ToolBox settings API
- [ ] 9. **M:N relationships** - many-to-many relationship design and deploy
